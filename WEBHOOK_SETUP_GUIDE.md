# 📘 Руководство по настройке Webhooks для Avito

## Что такое Webhook?

**Webhook** - это способ получения уведомлений от Avito в реальном времени. Вместо того, чтобы постоянно опрашивать API Avito на наличие новых сообщений, Avito сам отправляет уведомления на ваш сервер, когда происходят события (новые сообщения, изменения в чатах и т.д.).

## Зачем нужен Webhook?

✅ **Преимущества:**
- Мгновенные уведомления о новых сообщениях
- Меньше нагрузки на сервер (не нужно постоянно опрашивать API)
- Автоматическая синхронизация чатов
- Экономия API запросов

## Как настроить Webhook - Пошаговая инструкция

### Шаг 1: Определите URL вашего webhook

Ваш webhook URL должен быть доступен по HTTPS и иметь формат:
```
https://ваш-домен.com/webhook/avito
```

**Примеры:**
- `https://crm.example.com/webhook/avito`
- `https://yourdomain.ru/webhook/avito`

⚠️ **Важно:**
- URL должен быть доступен по **HTTPS** (не HTTP!)
- URL должен быть доступен из интернета (не localhost)
- Сервер должен отвечать 200 OK в течение 5 секунд

### Шаг 2: Зарегистрируйте Webhook через админ-панель

1. **Войдите как супер-админ** в систему
2. Перейдите в **Админ панель** → **Webhooks**
3. Заполните форму:
   - **URL Webhook**: `https://ваш-домен.com/webhook/avito`
   - **Типы событий**: выберите нужные
     - ✅ `message` - события сообщений (новые сообщения, обновления)
     - ✅ `chat` - события чатов (создание, обновление)
     - ⬜ `user` - события пользователей (опционально)
4. Нажмите **"Зарегистрировать Webhook"**

### Шаг 3: Проверьте регистрацию

После регистрации вы увидите информацию о текущем webhook:
- URL
- Статус (активен/неактивен)
- Типы событий
- Даты создания и обновления

### Шаг 4: Проверьте работу webhook

1. Отправьте тестовое сообщение в чат Avito
2. Проверьте логи сервера - должны появиться записи о получении webhook
3. Проверьте, что новое сообщение появилось в системе

## Что происходит после регистрации?

Когда Avito отправляет уведомление:
1. Avito делает POST запрос на ваш URL: `https://ваш-домен.com/webhook/avito`
2. Ваш сервер получает данные о событии
3. Система автоматически синхронизирует чаты и сообщения
4. Сервер отвечает 200 OK (если все успешно)

## Типы событий

### `message` - События сообщений
- Новое сообщение в чате
- Обновление сообщения
- Удаление сообщения

### `chat` - События чатов
- Создание нового чата
- Обновление чата
- Архивация чата
- Изменение статуса чата

### `user` - События пользователей
- Изменение информации о пользователе
- Блокировка/разблокировка пользователя

## Устранение проблем

### Webhook не работает

1. **Проверьте доступность URL:**
   ```bash
   curl -X POST https://ваш-домен.com/webhook/avito
   ```

2. **Проверьте логи сервера:**
   ```bash
   tail -f /var/www/tbg_improved/logs/app.log
   ```

3. **Проверьте, что URL использует HTTPS:**
   - Avito требует HTTPS для webhook
   - HTTP не будет работать

4. **Проверьте firewall:**
   - Убедитесь, что порт 443 (HTTPS) открыт
   - Проверьте, что сервер доступен из интернета

### Webhook получает ошибки

1. **Проверьте формат ответа:**
   - Сервер должен отвечать 200 OK
   - Ответ должен быть в формате JSON

2. **Проверьте обработку данных:**
   - Смотрите логи на наличие ошибок обработки
   - Убедитесь, что endpoint `/webhook/avito` работает

## Текущий endpoint для приема webhook

Ваш сервер уже настроен для приема webhook-запросов:
- **URL**: `/webhook/avito`
- **Метод**: POST
- **Обработка**: Автоматическая синхронизация чатов и сообщений

## Примеры использования

### Регистрация через UI (рекомендуется)
1. Откройте `/admin/webhooks`
2. Введите URL: `https://ваш-домен.com/webhook/avito`
3. Выберите типы событий
4. Нажмите "Зарегистрировать"

### Проверка текущего webhook
```bash
# Через API
curl -X GET https://ваш-домен.com/api/admin/webhooks \
  -H "Cookie: session=ваша-сессия"
```

### Удаление webhook
1. Откройте `/admin/webhooks`
2. Нажмите "Удалить" на текущем webhook

## Безопасность

⚠️ **Важно для безопасности:**
- Webhook URL должен быть защищен HTTPS
- Рекомендуется проверять подпись webhook (X-Signature)
- Ограничьте доступ к endpoint только для Avito IP (если возможно)

## Дополнительная информация

- Документация Avito: https://developers.avito.ru/api-catalog/messenger/documentation
- Webhook v3 API: https://developers.avito.ru/api-catalog/messenger/documentation#operation/postWebhookV3
