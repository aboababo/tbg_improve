# Инструкция по очистке неиспользуемых файлов на сервере

## Описание

Скрипты для удаления всех неиспользуемых файлов, которые были удалены локально:
- Тестовые файлы (test_*.py, TEST_*.py)
- Диагностические файлы (check_*, diagnose_*, debug_*, analyze_*)
- Временные скрипты (fix_*, clear_*, remove_*, backup_*, и т.д.)
- Временные .sh скрипты (CHECK_*, DEBUG_*, FIX_*, и т.д.)
- Временные .md файлы (кроме README.md и WEBHOOK_SETUP_GUIDE.md)
- Временные .bat/.ps1/.txt файлы (кроме requirements.txt)

## Использование

### Безопасный способ (рекомендуется)

1. **Сначала проверьте, что будет удалено (dry-run):**
   ```bash
   bash cleanup_unused_files_safe.sh --dry-run
   ```
   или
   ```bash
   bash cleanup_unused_files_safe.sh -n
   ```

2. **Если все правильно, выполните реальное удаление:**
   ```bash
   bash cleanup_unused_files_safe.sh
   ```

### Простой способ

```bash
bash cleanup_unused_files.sh
```

## Что НЕ будет удалено

Скрипт сохраняет все важные файлы:
- ✅ `README.md`
- ✅ `WEBHOOK_SETUP_GUIDE.md`
- ✅ `backend/requirements.txt`
- ✅ Основные скрипты запуска:
  - `start.sh`
  - `stop.sh`
  - `restart.sh`
  - `install.sh`
- ✅ Скрипты автосинхронизации:
  - `start_auto_sync.sh`
  - `stop_auto_sync.sh`
  - `start_sync_forever.sh`
  - `stop_sync.sh`
- ✅ `passenger_wsgi.py`
- ✅ Все файлы в `backend/` кроме временных скриптов
- ✅ Все файлы в `backend/templates/`
- ✅ Все файлы в `backend/services/`
- ✅ Все файлы в `backend/api/`

## Пример вывода

```
==========================================
Очистка неиспользуемых файлов на сервере
РЕЖИМ: ПРОВЕРКА (файлы не удаляются)
==========================================

Начинаем удаление неиспользуемых файлов...

--- Удаление тестовых Python файлов ---
🔍 Будет удален: ./test_all.py
🔍 Будет удален: ./test_api_endpoints.py
...

==========================================
ПРОВЕРКА ЗАВЕРШЕНА (файлы НЕ удалены)
==========================================
Найдено/удалено файлов: 165
Ошибок: 0
Не найдено: 12
```

## Важные замечания

⚠️ **ВНИМАНИЕ:**
- Скрипт удаляет файлы безвозвратно
- Рекомендуется сначала запустить с `--dry-run` для проверки
- Убедитесь, что вы находитесь в правильной директории проекта
- Сделайте резервную копию перед запуском, если сомневаетесь

## Восстановление

Если вы случайно удалили нужный файл:
1. Проверьте резервную копию (если есть)
2. Восстановите из git (если файл был в репозитории):
   ```bash
   git checkout HEAD -- путь/к/файлу
   ```

